import 'package:flutter/material.dart';

class ModelStepsColor {
  Color textColor;
  String text;
  Color buttonColor;
  String icon;

  ModelStepsColor(this.textColor, this.buttonColor, this.icon,this.text);
}
